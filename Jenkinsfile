node{
    
    if(env.BRANCH_NAME == 'main'){
        stage('main'){
            echo "Building Main Branch..."
        }
    }
    
    if(env.BRANCH_NAME == "dev"){
        stage('Dev'){
            echo "Building Dev branch"
        }
    }
}
